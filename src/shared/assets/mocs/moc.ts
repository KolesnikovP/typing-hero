export const mocTexts: Array<Array<string>> = [
  ['f the 2 same component is used as the child of multiple Routes at the same point in the component tree, React will see this as the same component instance and the component’s state will be preserved between route changes. '],
  ['Trolls are attacking  2 your comment section! A common way to deal with this situation is to remove all of the vowels from the trolls comments, neutralizing the threat Your task is to write a function that takes a string and return a new string with all vowels removed.'],
];

export const generateText = (): string => mocTexts[0][0];
